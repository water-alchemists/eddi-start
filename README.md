# EDDI Startup Script
Start up script to run on the Artik

## Setup after download
1. Make the startup script an executable
```chmod +x ./startup.sh```

2. Add a line calling this script to the .bash_profile to have it launch on sign in.

3. Restart to refresh